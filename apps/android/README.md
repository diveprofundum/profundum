# Android app

Native Android application that links to the shared core via UniFFI and uses the shared Compose Multiplatform UI module.

## Planned modules
- Onboarding + permissions
- Dashboard view
- Dive list + detail
- Sync view (BLE session)
